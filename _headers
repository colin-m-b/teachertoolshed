# Netlify headers. No build step: this file is deployed as-is from the repo root.

/*
  X-Content-Type-Options: nosniff
  Referrer-Policy: strict-origin-when-cross-origin
  X-Frame-Options: SAMEORIGIN
  Permissions-Policy: camera=(), microphone=(), geolocation=()
  Content-Security-Policy: default-src 'self'; script-src 'self' 'unsafe-inline' 'wasm-unsafe-eval' blob:; style-src 'self' 'unsafe-inline'; img-src 'self' data: blob:; font-src 'self'; worker-src 'self' blob:; connect-src 'self' blob: data:; frame-src https://www.youtube-nocookie.com; frame-ancestors 'self'; base-uri 'self'; form-action 'self'

# frame-src: the help pages' click-to-load video poster is the only frame, and
# it is created only when the teacher presses Play.

# When Pro goes on sale (js/toolshed-licence.js config.api set), the licence
# check is the ONE outside request the site makes, and it carries only the
# key. Add the merchant of record's API origin to connect-src, e.g.
#   connect-src 'self' blob: data: https://api.lemonsqueezy.com;
# and update privacy.html and the landing-page footer line the same day.
# When sync goes live (js/toolshed-sync.js client ids set), also add
#   https://www.googleapis.com https://graph.microsoft.com https://login.microsoftonline.com
# to connect-src. Those requests carry the teacher's backup to the
# teacher's own drive, and only after the teacher connects it.

# Long-lived, immutable assets. When a file here changes, change its name.
/css/fonts/*
  Cache-Control: public, max-age=31536000, immutable
/teacher-tools/vendor/*
  Cache-Control: public, max-age=31536000, immutable

# Everything else revalidates so a deploy is visible straight away.
/css/*
  Cache-Control: public, max-age=3600, must-revalidate
/js/*
  Cache-Control: public, max-age=3600, must-revalidate
/*.html
  Cache-Control: public, max-age=0, must-revalidate
